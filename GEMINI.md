# Antigravity Project Rules
Follow `AGENTS.md` as the shared project instruction entrypoint.

When starting a task:
1. Read `AGENTS.md`.
2. Use orchestrator at `Docs(Template)/2_System_Files/Agent_Prompts/orchestrator-agent.md`.
3. Load only current task card, relevant workflow, relevant role prompt, and relevant GDD/ADR/rules sections.

Do not load the entire `Docs(Template)/` folder unless requested.

