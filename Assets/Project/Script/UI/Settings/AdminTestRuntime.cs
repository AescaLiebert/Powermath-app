namespace PowerMath.UI.Settings
{
    // Kept as an empty source migration marker so an open Unity Editor can
    // retire the former session-only admin runtime without a stale compile item.
    internal static class AdminRuntimeMigrationMarker
    {
    }
}
