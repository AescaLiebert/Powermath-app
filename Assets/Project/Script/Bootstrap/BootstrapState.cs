namespace PowerMath.Bootstrap
{
    public enum BootstrapState
    {
        CheckingSession,
        LoadingPlayer,
        Ready,
        LoadingScene,
        Recovering,
        AuthenticationRequired,
        IncompatibleClient
    }
}
