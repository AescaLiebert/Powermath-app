namespace PowerMath.Bootstrap
{
    public enum BootstrapState
    {
        CheckingVersion,
        CheckingSession,
        LoadingPlayer,
        Ready,
        LoadingScene,
        Recovering,
        AuthenticationRequired,
        IncompatibleClient,
        MaintenanceMode
    }
}
